/* data/catalog — legacy adapter: renders the pre-rework page `pages/Catalog` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/Catalog";
