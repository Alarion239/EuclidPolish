/* sky/catalog-eval — legacy adapter: renders the pre-rework page `pages/Evaluation` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/Evaluation";
