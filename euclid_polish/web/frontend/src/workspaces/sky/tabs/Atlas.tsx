/* sky/atlas — legacy adapter: renders the pre-rework page `pages/JwstEuclid` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/JwstEuclid";
