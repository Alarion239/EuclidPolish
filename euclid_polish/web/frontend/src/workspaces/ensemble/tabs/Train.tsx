/* ensemble/train — legacy adapter: renders the pre-rework page `pages/TrainMembers` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/TrainMembers";
