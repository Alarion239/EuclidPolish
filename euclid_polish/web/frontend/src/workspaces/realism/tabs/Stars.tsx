/* realism/stars — legacy adapter: renders the pre-rework page `pages/StarDistribution` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/StarDistribution";
