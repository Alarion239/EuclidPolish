/* settings/config — legacy adapter: renders the pre-rework page `pages/Config` until
   the phase-3 workspace owner replaces this tab (plan WP-F T8). */
export { default } from "../../../pages/Config";
