dsa-example/*
dsa-example-cache/*
monopsfs_euclidlike/*
euclid256*
EuclidPSF*
.claude*
monopsfs*
*.png
*.zip
*.gz

# Python / IDE / OS
__pycache__/
*.py[cod]
.DS_Store
.idea/

# Reference papers / PDFs — keep in a separate docs share, not source control
*.pdf

# Data: only data/euclid_psf/ is tracked; everything else is generated/local.
data/*
!data/euclid_psf/

# Experiment-tracking store: mirrored to FASRC holylabs, never committed.
/tracking/

# Time-travel sandboxes (git worktrees + isolated data/ckpt). Disposable.
/.timetravel/

# Vendored reference code (heavy data examples)
scripts/polish-pub/

# Secrets / credentials
.env
.env.*
*.credentials
.euclid_credentials
euclid_credentials*
